name: Deploy Web to Azure App Service

on:
  push:
    branches: [main]
    paths:
      - 'web/**'
      - 'package.json'
      - 'package-lock.json'
      - 'tsconfig.json'
  workflow_dispatch:

env:
  AZURE_WEBAPP_NAME: dvlp-ksef
  AZURE_FUNCTIONAPP_NAME: dvlp-ksef-func
  NODE_VERSION: '20.x'

permissions:
  id-token: write
  contents: read

jobs:
  deploy-web:
    name: Build & Deploy Web
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: .

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Typecheck (web)
        run: npm run typecheck --workspace=web

      - name: Test (web)
        run: npm test --workspace=web

      - name: Azure Login (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Resolve API URL
        id: api-url
        run: |
          HOSTNAME=$(az functionapp show --name ${{ env.AZURE_FUNCTIONAPP_NAME }} --resource-group rg-ksef --query properties.defaultHostName -o tsv)
          echo "value=https://${HOSTNAME}" >> "$GITHUB_OUTPUT"
          echo "Resolved API_URL: https://${HOSTNAME}"

      - name: Build deploy artifact
        working-directory: web
        run: node scripts/build-deploy.mjs
        env:
          API_URL: ${{ steps.api-url.outputs.value }}
          NEXT_PUBLIC_AZURE_CLIENT_ID: ${{ secrets.NEXT_PUBLIC_AZURE_CLIENT_ID }}
          NEXT_PUBLIC_AZURE_TENANT_ID: ${{ secrets.NEXT_PUBLIC_AZURE_TENANT_ID }}
          NEXT_PUBLIC_API_SCOPE: ${{ secrets.NEXT_PUBLIC_API_SCOPE }}
          NEXT_PUBLIC_ADMIN_GROUP: ${{ secrets.NEXT_PUBLIC_ADMIN_GROUP }}
          NEXT_PUBLIC_USER_GROUP: ${{ secrets.NEXT_PUBLIC_USER_GROUP }}

      - name: Deploy to Azure App Service
        uses: azure/webapps-deploy@v3
        with:
          app-name: ${{ env.AZURE_WEBAPP_NAME }}
          package: web/.deploy

      - name: Smoke test
        continue-on-error: true
        run: |
          HOSTNAME=$(az webapp show --name ${{ env.AZURE_WEBAPP_NAME }} --resource-group rg-ksef --query defaultHostName -o tsv)
          echo "Waiting for deployment to stabilize..."
          sleep 30
          STATUS=$(curl -s -o /dev/null -w "%{http_code}" "https://${HOSTNAME}/" || echo "000")
          if [ "$STATUS" -ge 200 ] && [ "$STATUS" -lt 400 ]; then
            echo "✓ Smoke test passed (HTTP $STATUS)"
          else
            echo "⚠ Smoke test returned HTTP $STATUS — check deployment"
            exit 1
          fi
