{
  "name": "@dvlp-ksef/api",
  "version": "0.2.0",
  "description": "Azure Functions API for KSeF integration",
  "main": "dist/src/functions/index.js",
  "scripts": {
    "build": "tsc && npm run copy-prompts",
    "copy-prompts": "node -e \"const fs=require('fs');const path=require('path');const src='src/lib/prompts';const dest='dist/src/lib/prompts';fs.readdirSync(src).filter(f=>f.endsWith('.prompt.md')).forEach(f=>{fs.mkdirSync(dest,{recursive:true});fs.copyFileSync(path.join(src,f),path.join(dest,f));console.log('Copied:',f)})\"",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build",
    "start": "func start",
    "dev": "npm run start",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "lint:fix": "eslint src --ext .ts --fix",
    "seed:testdata": "tsx scripts/seed-testdata.ts",
    "seed:mpk": "tsx scripts/seed-mpk-centers.ts",
    "migrate:mpk": "tsx scripts/migrate-invoices-to-mpk-centers.ts",
    "seed:corrections": "tsx scripts/seed-corrective-invoices.ts",
    "cleanup:testdata": "tsx scripts/cleanup-testdata.ts"
  },
  "dependencies": {
    "@azure/functions": "^4.5.0",
    "@azure/identity": "^4.13.1",
    "@azure/keyvault-secrets": "^4.8.0",
    "@azure/msal-node": "^3.8.10",
    "cookie": "^1.1.1",
    "fast-xml-parser": "^5.5.8",
    "jose": "^6.2.2",
    "openai": "^6.32.0",
    "pdf-parse": "^2.4.5",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/cookie": "^1.0.0",
    "@types/node": "^25.5.0",
    "@types/pdf-parse": "^1.1.5",
    "@typescript-eslint/eslint-plugin": "^8.57.1",
    "@typescript-eslint/parser": "^8.57.1",
    "azure-functions-core-tools": "^4.8.0",
    "dotenv": "^17.3.1",
    "eslint": "^10.1.0",
    "rimraf": "^6.1.3",
    "tsx": "^4.21.0",
    "typescript": "^5.7.0",
    "vitest": "^4.1.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
